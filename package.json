{
  "name": "redteam-loop",
  "version": "0.1.0",
  "description": "An AI DevSecOps loop: fires OWASP-classified attacks at a running target, triages what lands, proposes a patch, and verifies the patch actually closes it. Proposes only — a human merges.",
  "type": "module",
  "bin": { "redteam": "./src/cli.js" },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "scan": "node src/cli.js scan",
    "loop": "node src/cli.js loop"
  },
  "dependencies": { "@anthropic-ai/sdk": "^0.70.0" },
  "license": "MIT"
}
